---
"@fiscozen/chat-container": patch
---

Update chat container to support preformatted message display. Message content is now rendered inside a `<pre>` element with appropriate styling (`whitespace-pre-wrap`, sans font) so that line breaks and spacing in chat messages are preserved.
