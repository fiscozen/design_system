---
"@fiscozen/table": patch
---

Fix accordion rows without subRows: hide expand icon but keep left column cell for alignment. Fix subrow actions to receive subrow data instead of parent row data. Use fixed 40px column for accordion left column.
Fixed an issue with accordion variant in combination with rows using custom ids.
