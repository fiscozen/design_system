---
"@fiscozen/table": minor
---

Add URL sync props (paginationSyncUrl, paginationUrlKey, urlKeyPrefix, environment) to FzTable, exposing FzPagination URL sync capabilities with support for multiple tables on the same page via key prefixing
