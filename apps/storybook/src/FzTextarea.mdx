import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Documentation/Form/FzTextarea" />

# FzTextarea

Multi-line text input component with label, validation states, resize control, and full WCAG 2.1 AA accessibility support.

## Features

- Resize control (none, vertical, horizontal, all)
- `bare` variant: no box of its own (no border, background, padding or minimum height) and no font
  size, so a container drawn by the caller becomes the visible field and owns the type — the shape a
  single-line composer bar needs
- Auto-height mode: textarea grows/shrinks with content, optional max-row cap
- Error and valid states with visual feedback
- Error messages via FzAlert component (circle-xmark icon, consistent with FzInput)
- Custom focus styling (blue border on focus, error border maintained on error+focus)
- `focusAffordance`: in the `bare` variant, the focus indicator can be delegated to the container
  that draws the field, for boxes with padding of their own
- Help text and error messages with multiline wrapping support
- Required, disabled, and readonly states with proper visual cues
- Combined states support (error+disabled, required+help, disabled+compiled)
- Full ARIA accessibility (aria-required, aria-invalid, aria-disabled, aria-labelledby, aria-describedby)
- Error messages announced via role="alert"
- Auto-generated IDs for label association
- Exposed `textareaRef` for programmatic focus
- Native event forwarding via `v-bind="$attrs"` (inheritAttrs: false)

## Installation

```bash
npm install @fiscozen/textarea
```

## Basic Usage

```vue
<script setup>
import { FzTextarea } from '@fiscozen/textarea'
import { ref } from 'vue'

const notes = ref('')
</script>

<template>
  <FzTextarea label="Notes" v-model="notes" />
</template>
```

## Props

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>label</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>Text label displayed above the textarea (required)</td>
    </tr>
    <tr>
      <td><code>id</code></td>
      <td><code>string</code></td>
      <td>auto-generated</td>
      <td>HTML id attribute. Falls back to auto-generated ID for label association.</td>
    </tr>
    <tr>
      <td><code>name</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>Form field name for submission</td>
    </tr>
    <tr>
      <td><code>size</code></td>
      <td><code>'sm' | 'md' | 'lg'</code></td>
      <td><code>'md'</code></td>
      <td><strong>Deprecated.</strong> Accepted but ignored. The textarea always uses text-base (16px). Will be removed in the next major version.</td>
    </tr>
    <tr>
      <td><code>placeholder</code></td>
      <td><code>string</code></td>
      <td>-</td>
      <td>Placeholder text shown when textarea is empty</td>
    </tr>
    <tr>
      <td><code>required</code></td>
      <td><code>boolean</code></td>
      <td><code>false</code></td>
      <td>Marks field as required. Adds asterisk to label.</td>
    </tr>
    <tr>
      <td><code>disabled</code></td>
      <td><code>boolean</code></td>
      <td><code>false</code></td>
      <td>Disables interaction and applies muted styling</td>
    </tr>
    <tr>
      <td><code>readonly</code></td>
      <td><code>boolean</code></td>
      <td><code>false</code></td>
      <td>Prevents editing while keeping field focusable</td>
    </tr>
    <tr>
      <td><code>error</code></td>
      <td><code>boolean</code></td>
      <td><code>false</code></td>
      <td>Enables error state with red border. Paired with <code>errorMessage</code> slot.</td>
    </tr>
    <tr>
      <td><code>valid</code></td>
      <td><code>boolean</code></td>
      <td><code>false</code></td>
      <td>Shows success checkmark icon</td>
    </tr>
    <tr>
      <td><code>variant</code></td>
      <td><code>'default' | 'bare'</code></td>
      <td><code>'default'</code></td>
      <td>Visual variant. <code>default</code> draws the field (border, background, padding, 77px minimum height); <code>bare</code> draws no box at all, leaving it to the surrounding container. See <a href="#bare-variant-composer-bars">Bare Variant</a>.</td>
    </tr>
    <tr>
      <td><code>focusAffordance</code></td>
      <td><code>'field' | 'container'</code></td>
      <td><code>'field'</code></td>
      <td><code>bare</code> only. Which element draws the focus indicator. <code>field</code> draws a 2px <code>blue-600</code> <code>focus-visible</code> outline on the textarea; <code>container</code> draws none and requires the caller to draw one on its own box — dropping it outright fails WCAG 2.4.7. See <a href="#focus-affordance-who-draws-the-ring">Focus Affordance</a>.</td>
    </tr>
    <tr>
      <td><code>resize</code></td>
      <td><code>'none' | 'vertical' | 'horizontal' | 'all'</code></td>
      <td><code>'all'</code> (<code>'none'</code> when <code>variant="bare"</code>)</td>
      <td>Controls resize behavior of the textarea</td>
    </tr>
    <tr>
      <td><code>rows</code></td>
      <td><code>number</code></td>
      <td><code>2</code> (<code>1</code> when <code>variant="bare"</code>)</td>
      <td>Number of visible text rows</td>
    </tr>
    <tr>
      <td><code>cols</code></td>
      <td><code>number</code></td>
      <td>-</td>
      <td>Visible width in average character widths</td>
    </tr>
    <tr>
      <td><code>minlength</code></td>
      <td><code>number</code></td>
      <td>-</td>
      <td>Native minlength constraint</td>
    </tr>
    <tr>
      <td><code>maxlength</code></td>
      <td><code>number</code></td>
      <td>-</td>
      <td>Native maxlength constraint</td>
    </tr>
    <tr>
      <td><code>autoHeight</code></td>
      <td><code>boolean</code></td>
      <td><code>false</code></td>
      <td>Enables automatic height adjustment based on content. Vertical resize is disabled when active. Must be set at mount time (runtime changes not supported).</td>
    </tr>
    <tr>
      <td><code>maxRows</code></td>
      <td><code>number</code></td>
      <td>-</td>
      <td>Maximum visible rows before a scrollbar appears. Only effective when <code>autoHeight</code> is true; a console warning is emitted otherwise.</td>
    </tr>
  </tbody>
</table>

## Events

<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>Payload</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>update:modelValue</code></td>
      <td><code>string</code></td>
      <td>Emitted when text content changes (v-model)</td>
    </tr>
  </tbody>
</table>

All native DOM events (<code>blur</code>, <code>focus</code>, <code>paste</code>, <code>keydown</code>, etc.) are automatically forwarded to the native <code>&lt;textarea&gt;</code> element via <code>v-bind="$attrs"</code>. You can listen to any native event directly on the component:

```vue
<template>
  <FzTextarea label="Notes" @blur="onBlur" @keydown.enter="onEnter" />
</template>
```

Any extra HTML attributes (e.g. <code>data-*</code>, <code>class</code>, <code>style</code>) are also applied to the native textarea, not the wrapper div.

## Exposed Refs

<table>
  <thead>
    <tr>
      <th>Ref</th>
      <th>Type</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>textareaRef</code></td>
      <td><code>HTMLTextAreaElement | null</code></td>
      <td>Reference to the native textarea element for programmatic focus or measurement</td>
    </tr>
  </tbody>
</table>

```vue
<script setup>
import { ref } from 'vue'
import { FzTextarea } from '@fiscozen/textarea'

const textarea = ref()

function focusTextarea() {
  textarea.value?.textareaRef?.focus()
}
</script>

<template>
  <FzTextarea ref="textarea" label="Notes" v-model="notes" />
  <button @click="focusTextarea">Focus textarea</button>
</template>
```

## Slots

<table>
  <thead>
    <tr>
      <th>Slot</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>errorMessage</code></td>
      <td>Error content displayed below textarea via FzAlert when <code>error</code> is true. Takes precedence over <code>helpText</code>.</td>
    </tr>
    <tr>
      <td><code>helpText</code></td>
      <td>Help content displayed below textarea. Hidden when error message is shown. Greyed out when disabled or readonly.</td>
    </tr>
  </tbody>
</table>

## Examples

### With Validation

```vue
<script setup>
import { FzTextarea } from '@fiscozen/textarea'
import { ref, computed } from 'vue'

const description = ref('')
const hasError = computed(() => description.value.length > 0 && description.value.length < 10)
</script>

<template>
  <FzTextarea
    label="Description"
    v-model="description"
    :error="hasError"
    required
  >
    <template #errorMessage>Description must be at least 10 characters</template>
  </FzTextarea>
</template>
```

### With Help Text

```vue
<template>
  <FzTextarea
    label="Bio"
    v-model="bio"
    :maxlength="500"
    resize="vertical"
  >
    <template #helpText>Tell us about yourself (max 500 characters)</template>
  </FzTextarea>
</template>
```

### Disabled State

```vue
<template>
  <FzTextarea
    label="Locked Field"
    modelValue="This content cannot be edited"
    disabled
  />
</template>
```

### Readonly State

```vue
<template>
  <FzTextarea
    label="Terms and Conditions"
    modelValue="Lorem ipsum dolor sit amet..."
    readonly
    :rows="6"
  />
</template>
```

### Vertical-only Resize

```vue
<template>
  <FzTextarea
    label="Vertical-only resize"
    v-model="text"
    resize="vertical"
    :rows="4"
  />
</template>
```

### Error with Pre-filled Content

```vue
<template>
  <FzTextarea
    label="Description"
    v-model="description"
    :error="true"
  >
    <template #errorMessage>Content does not meet requirements</template>
  </FzTextarea>
</template>
```

### Valid with Content

```vue
<template>
  <FzTextarea
    label="Notes"
    v-model="notes"
    :valid="true"
  />
</template>
```

### Required with Help Text

```vue
<template>
  <FzTextarea
    label="Comments"
    v-model="comments"
    required
  >
    <template #helpText>This field is mandatory</template>
  </FzTextarea>
</template>
```

### Disabled with Value

```vue
<template>
  <FzTextarea
    label="Archived Notes"
    modelValue="This content is archived and cannot be edited"
    disabled
  />
</template>
```

## Auto Height

When `autoHeight` is `true`, the textarea automatically grows as the user types and shrinks when content is removed. The `rows` prop defines the minimum visible height and `maxRows` caps the growth; once content exceeds `maxRows` a vertical scrollbar appears.

### Basic Auto Height

```vue
<template>
  <FzTextarea
    label="Description"
    v-model="text"
    autoHeight
    :rows="2"
  />
</template>
```

### Auto Height with Maximum Rows

```vue
<template>
  <FzTextarea
    label="Notes"
    v-model="notes"
    autoHeight
    :rows="2"
    :maxRows="10"
  />
</template>
```

### Resize Behavior

Vertical resize is automatically disabled when `autoHeight` is active because the component controls the height programmatically. Horizontal resize is preserved if explicitly set (via `resize="horizontal"` or the default `resize="all"`).

A console warning is emitted if:
- `maxRows` is used without `autoHeight`
- `autoHeight` is active with a resize value that includes vertical resizing (`all` or `vertical`)

### Anchoring (Growth Direction)

The textarea always grows **downward** (anchored at the top). This is the native browser behavior: as new lines are added, the bottom edge of the textarea moves down while the top edge stays in place.

If you need the textarea to appear to grow **upward** (e.g. a chat-like input pinned at the bottom of a container), the component itself does not need to change. Instead, use CSS on the parent container:

```vue
<template>
  <div class="flex flex-col justify-end h-[400px]">
    <FzTextarea
      label="Message"
      v-model="message"
      autoHeight
      :rows="2"
      :maxRows="8"
    />
  </div>
</template>
```

With `flex-col justify-end`, the textarea sits at the bottom of the container. As it grows, the flex layout pushes the content above it upward, creating the illusion of upward growth. The textarea itself is still anchored at its top edge.

### Important Notes

- `autoHeight` must be set at mount time. Changing it at runtime is not supported.
- When `autoHeight` is enabled and the textarea has pre-filled content (via `v-model`), the height is adjusted on mount.

## Bare Variant (composer bars)

`variant="bare"` turns off everything the component draws around the text: border, background,
padding, corner radius, minimum height, minimum width — and the font size. What stays is the field
itself — the label, the help text, the error ARIA, `autoHeight`, `maxRows` and every native
attribute forwarded through `$attrs`.

Use it whenever the box is already drawn by something else: a composer bar, a toolbar, a cell, an
inline editor. Before this variant existed, the only way to get that shape was to re-declare the
component's own styling from the outside with `!`-important utilities.

```vue
<script setup>
import { FzTextarea } from '@fiscozen/textarea'
import { ref } from 'vue'

const draft = ref('')
</script>

<template>
  <!-- The container is the visible field: it owns the background, the padding,
       the radius and the type. The textarea only carries the text, and the
       counter sits inside the same box on the same scale. -->
  <div class="flex min-h-[44px] flex-col justify-end gap-4 rounded bg-grey-100 px-10 py-8 text-sm leading-5">
    <FzTextarea
      v-model="draft"
      variant="bare"
      auto-height
      :max-rows="6"
      :maxlength="200"
      placeholder="Scrivi un messaggio..."
      aria-label="Scrivi un messaggio"
    />
    <p class="self-end text-grey-500">{{ draft.length }}/200</p>
  </div>
</template>
```

Notes:

- **Defaults shift with the variant.** `rows` defaults to `1` and `resize` to `'none'`, so the field
  starts on a single line and no resize grabber breaks the container's shape. Both can still be set
  explicitly.
- **Type is inherited, not declared.** The bare field sets no font size, so `text-sm leading-5` on
  the box carries the field and everything else in it on one scale. (Tailwind's preflight gives a
  `textarea` `font-size: 100%` and `line-height: inherit`, which is what makes this work.) The
  default variant keeps its fixed `text-base`.
- **`autoHeight` + `maxRows` work unchanged.** The ceiling is measured from the live line height and
  padding, which is simply zero here: with `maxRows="6"` and a 20px line height the field stops at
  120px and starts scrolling. If the inherited line height computes to `normal`, the ceiling falls
  back to the browser's ~1.2 ratio rather than going unbounded.
- **The error state has no border to colour.** `error` still drives `aria-invalid` and the
  `errorMessage` slot, and the text turns grey when disabled or readonly, but any visual error
  affordance on the box is the caller's to draw (the composer in the chat widget, for instance, puts
  the message above the bar).
- **Focus stays visible.** Either on the field or on your box — see below.

## Focus Affordance (who draws the ring)

The bare variant strips the border, so the border-colour focus cue goes with it. What replaces it
is a `focus-visible` outline on the field, and `focusAffordance` decides whether the component
keeps drawing it or hands it to you.

| Value | The component draws | You must draw |
| --- | --- | --- |
| `'field'` *(default)* | a 2px `blue-600` outline on the textarea, offset by 2px | nothing |
| `'container'` | nothing — the browser's own ring is suppressed too | a focus indicator on your box |

Reach for `'container'` when the box has padding of its own. An outline on the field is offset from
the *field*, so inside a `px-10 py-8` container it lands **inside** the grey box and reads as a
field within a field. Moving it to the box puts it where the visible field actually is:

```vue
<template>
  <!-- The box is the visible field, so the box takes the focus ring. -->
  <div
    class="flex min-h-[44px] flex-col justify-end gap-4 rounded bg-grey-100 px-10 py-8 text-sm leading-5
           focus-within:outline focus-within:outline-2 focus-within:outline-blue-600"
  >
    <FzTextarea
      v-model="draft"
      variant="bare"
      focus-affordance="container"
      auto-height
      :max-rows="6"
      aria-label="Scrivi un messaggio"
    />
  </div>
</template>
```

Notes:

- **The prop moves an obligation, it does not cancel one.** `'container'` suppresses every focus
  ring on the field, the browser's included — otherwise the native ring would simply replace the
  one the component stopped drawing. A field left with no visible indicator anywhere is a
  **WCAG 2.4.7 (Focus Visible)** failure. The `focus-within:` treatment on your box is the other
  half of the prop, not decoration.
- **The component cannot check that you honoured it**, which is why the default stays on `'field'`:
  a caller who has never heard of the prop keeps a visible focus indicator.
- **Use the prop, not `!important`.** Suppressing the outline from the call site takes
  `!focus-visible:outline-none` — an override of the component's own styling, and the shape the
  bare variant exists to remove.
- **`'field'` and unset are identical.** Passing `'field'` explicitly changes nothing.
- **It only applies to `variant="bare"`.** The default variant recolours its own border; setting
  `focusAffordance` there emits a runtime warning and has no effect.

## Accessibility

FzTextarea meets WCAG 2.1 AA standards:

- **Focus indicator**: Custom blue border (`blue-600`) on focus replaces browser default outline. Error state maintains error border color on focus (`semantic-error-300`).
- **Focus indicator in the `bare` variant**: with no border to recolour, the cue is *replaced*, not removed — by default a 2px `blue-600` `focus-visible` outline is drawn on the field itself. If your container draws its own focus treatment, use `focusAffordance="container"` rather than an `!important` override; either way the indicator has to exist somewhere, because dropping it outright fails WCAG 2.4.7. See <a href="#focus-affordance-who-draws-the-ring">Focus Affordance</a>.
- **ARIA attributes**: `aria-required`, `aria-invalid`, `aria-disabled` always present with string values ("true"/"false") for screen reader compatibility
- **aria-describedby**: Links textarea to error or help message when present
- **role="alert"**: Error messages are announced immediately by screen readers
- **aria-labelledby**: References the label element ID for stronger screen reader association
- **Label association**: Label is always associated with textarea via `for`/`id` binding and `aria-labelledby` (auto-generated ID fallback)
- **Decorative icons**: Check icon and FzAlert error icon have `aria-hidden="true"`
- **Combined states**: Error+disabled, required+help, and other combinations maintain correct ARIA semantics
- **Keyboard navigation**: Full keyboard support (Tab to focus, type to input)

## Best Practices

- Always provide a descriptive `label` for accessibility
- Use `helpText` slot to guide users on expected input format
- Use `errorMessage` slot paired with `error` prop for validation feedback
- Combine `required` with `helpText` slot to explain what the field needs
- Set `resize="vertical"` for most use cases to prevent horizontal overflow (unless using `autoHeight`)
- Use `rows` to set a sensible initial height based on expected content length
- Use `autoHeight` with `maxRows` when the textarea should grow with content but stay bounded
- Prefer `autoHeight` over manual resize for a smoother user experience in forms
- Use `maxlength` to enforce content limits
- Reach for `variant="bare"` instead of overriding the component's box from the outside; set the type on the container rather than on the field, and keep the label or an `aria-label` even when the design has no visible label
- When showing pre-filled disabled fields, ensure the value conveys why the field is locked

## Related Components

- **FzInput**: Single-line text input with icon support and floating label variant
