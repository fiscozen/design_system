/**
 * @fiscozen/typeahead
 *
 * @deprecated Use `FzSelect` from `@fiscozen/select` with `filterable: true`
 * (and optionally `filterFn`) instead. See the package README for the migration guide.
 */
export { default as FzTypeahead } from "./FzTypeahead.vue";

export * from "./types";
