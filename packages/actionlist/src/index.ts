/**
 * @fiscozen/actionlist
 *
 * @deprecated Deprecated for external use — internal-only until consumers migrate.
 * New code must use `@fiscozen/action` instead (`FzActionList` + `FzActionSection`
 * + `FzAction`). This package stays published because `@fiscozen/table` still
 * depends on it. See the package README for the migration guide.
 */
export { default as FzActionlist } from './FzActionlist.vue'

export * from './types'
